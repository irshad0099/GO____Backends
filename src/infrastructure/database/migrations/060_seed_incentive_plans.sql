-- 060_seed_incentive_plans.sql
-- Deprecated. The seeds previously here targeted the old incentive schema
-- that was dropped in 065_rebuild_incentives.sql. The default plan is now
-- seeded inline in 065 so this file is intentionally a no-op.
SELECT 1;
